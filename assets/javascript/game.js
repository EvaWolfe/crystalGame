// Rupee Game //

// Computer has to randomly select a number for the user to match. //
// The random number shown at the start of the game should be between 19 - 120. //
var compRandNum = Math.floor(Math.random() * 102) + 19;
//Figure out how to store this number//

// Computer has to randomly assign the numbers (between 1 - 12) to the Rupees the user will be clicking. //
// Each Rupee should have a random hidden value between 1 - 12. //

var rupeeRandNum = Math.floor(Math.random() * 12) + 1;

for (var i = 0; i < array.length; i++) {
  array[i]
}
// User needs to be able to click on the four Rupees to be able to match the number generated by the computer. //
// Once Rupees are clicked by the user, the score counter will need to update. //
// Game needs to be able to track wins and losses.  //
// Game needs to restart after a player wins or losses. //
// When the game restarts, a new random number needs to be assigned. //
// When the game restarts, the Rupees need to have new random numbers assigned. //
// The user's score also needs to be reset. //
// The number of games a player has won or lost needs to also be displayed and this information needs to be kept even when the game restarts. //

// $(document).ready(function() {
// 	console.log("Ready!");
// });
//
//

// 	var blue = document.getElementById("blueRupee");
// 	var green = document.getElementById("greenRupee");
// 	var purple = document.getElementById("purpleRupee");
// 	var yellow = document.getElementById("yellowRupee");
//
// 	function rupeeClick(event) {
// 		alert("clicked");
// 		console.log(event);




	// blue.onclick = rupeeClick;
